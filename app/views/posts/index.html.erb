<h1> Posts </h1>

<div>
    <% @posts.each do |p|%>
    <div><%= p.body %></div>
    <div><%= p.author.name %></div><br>
    <% end %>
</div>

<%= button_to 'Sign out', destroy_user_session_path, method: :delete %>